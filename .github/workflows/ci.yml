# 템포루틴 CI (MASTER §5.10)
# 잡 1: TempoCore swift test — 순수 Foundation이라 Linux 러너(공식 Swift 컨테이너)로 분 단위 실행.
# 잡 2(앱 빌드·TestFlight)는 빌드 2에서 활성화 — macOS 러너 이미지·Xcode 버전은 그 시점에 실측(§5.10 추측 금지).

name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  tempocore-tests:
    name: TempoCore swift test (Linux)
    runs-on: ubuntu-latest
    container: swift:latest
    steps:
      - uses: actions/checkout@v4
      - name: Swift version
        run: swift --version
      - name: Build & test
        run: swift test --package-path TempoCore

  # ── 빌드 2에서 활성화 (게이트: Apple Developer + ASC API 키 → GH Secrets) ──
  # app-build:
  #   name: App build + simulator tests (macOS)
  #   runs-on: macos-latest   # ⚠ 이미지·Xcode 버전 실측 후 고정할 것
  #   steps:
  #     - uses: actions/checkout@v4
  #     - run: xcodegen generate          # project.yml → .xcodeproj
  #     - run: xcodebuild -scheme TempoRoutine -destination '...' test
  #     - # TestFlight 업로드: fastlane pilot / ASC API 키 = secrets.ASC_KEY_ID 등
